# Example inputs.dat configuration for FR-IGR
# This file uses standard INI format. Comments start with #.

[Physics]
# GAMMA: Ratio of specific heats (1.4 for air)
GAMMA = 1.4

# IC_TYPE: Initial Condition. Options: RIEMANN_2D_C3, BLAST, SINE_WAVE, UNIFORM, FREESTREAM
IC_TYPE = BLAST

# Freestream values (used if IC_TYPE = FREESTREAM or for certain BCs)
RHO_INF = 1.0
U_INF = 0.0
V_INF = 0.0
P_INF = 1.0

[Solver]
# P_DEG: Polynomial degree of elements. 0 = Finite Volume, 1 = Linear, 2 = Quadratic
P_DEG = 1

# CFL: Courant-Friedrichs-Lewy number for time step scaling
CFL = 1.0

# T_FINAL: Simulation end time
T_FINAL = 0.3

# NUM_THREADS: Number of OpenMP threads to use
NUM_THREADS = 4

# ENABLE_MULTIRATE: Enable time-accurate multirate sub-cycling.
ENABLE_MULTIRATE = false

# MAX_MULTIRATE_LEVEL: Max power-of-two level for multirate sub-cycling.
MAX_MULTIRATE_LEVEL = 3

[NavierStokes]
ENABLE_NS = true
RE = 100.0
PR = 0.72
MACH_REF = 5.0
NS_BR2_ETA = 1.0


[Regularization]
# ENABLE_IGR: Toggle Isotropic Gradient Regularization (artificial viscosity)
ENABLE_IGR = false

# ALPHA_SCALE: Viscosity scaling coefficient. Higher = more diffusion.
ALPHA_SCALE = 0.5

# IGR_GRADIENT_TYPE: "LOCAL" (interior points only) or "CORRECTED" (BR2 interface lifting)
IGR_GRADIENT_TYPE = LOCAL

# IGR_TYPE: "ELLIPTIC" (implicit ADI solve) or "PARABOLIC" (explicit evolution)
IGR_TYPE = PARABOLIC

# IGR_TAU_R: Relaxation time for parabolic mode.
IGR_TAU_R = 0.1

# IGR_BR2_ETA: BR2 penalty parameter for stability. Usually 1.0 to 3.0.
IGR_BR2_ETA = 1.0

# IGR_SUB_ITERS: Number of parabolic sub-iterations per flow step.
# If set to 0, the solver will dynamically calculate the number of sub-iterations
# required to satisfy the stability CFL limit of the diffusive step, meaning the
# global time step is not restricted by IGR.
IGR_SUB_ITERS = 0

# USE_DUCROS_SWITCH: Enable Ducros switch to scale down and suppress artificial viscosity in shear layers (vortical regions).
USE_DUCROS_SWITCH = false

# USE_PRESSURE_SENSOR: Use pressure-jump shock sensor instead of default density-gradient sensor.
USE_PRESSURE_SENSOR = false

# USE_MOMENTUM_DIV: Use momentum divergence for the compression check instead of velocity divergence.
USE_MOMENTUM_DIV = false

# USE_PRESSURE_SOURCE_CAP: Cap shock sensor source by local pressure (S <= SOURCE_CAP_COEFF * P) to stabilize strong shocks.
USE_PRESSURE_SOURCE_CAP = true

# USE_PRESSURE_FIELD_CAP: Cap the final resolved entropic pressure field by local pressure (Sigma <= P) downstream of the source term.
USE_PRESSURE_FIELD_CAP = true

# SOURCE_CAP_COEFF: Tuning coefficient for pressure-bounded source capping.
SOURCE_CAP_COEFF = 1.0

# IGR_DIVERGENCE_THRESHOLD: Divergence threshold for shock sensor activation. 
# Regularizer only activates when divergence < IGR_DIVERGENCE_THRESHOLD (must be compressive, e.g., 0.0 or slightly negative like -1e-3).
# Default of 1.0e99 disables the compression filter.
IGR_DIVERGENCE_THRESHOLD = 1.0e99

# IGR_SENSOR_THRESHOLD: Minimum value of raw sensor magnitude (tr(Du^2) + tr^2(Du)) to trigger regularizer.
# Default of -9.0e99 disables the sensor threshold filter.
IGR_SENSOR_THRESHOLD = -9.0e99

# IGR_SUB_ITER_TOL: Convergence tolerance for IGR sub-iterations.
# If greater than 0.0, sub-iterations run dynamically until the L1 difference between iterations
# drops below this tolerance, up to a maximum limit (IGR_SUB_ITERS or fallback 1000).
# Default of 0.0 disables convergence checking (runs exactly IGR_SUB_ITERS).
IGR_SUB_ITER_TOL = 0.0

[Stabilization]
# ENABLE_POS_LIMITER: Zhang-Shu density and pressure positivity limiter.
ENABLE_POS_LIMITER = true

# POS_LIMITER_EPS: Minimum value floor for density and pressure.
POS_LIMITER_EPS = 1e-10

# ENABLE_ENTROPY_LIMITER: Prevents unphysical entropy undershoots.
ENABLE_ENTROPY_LIMITER = true

# ENTROPY_LIMITER_EPS: Minimum specific entropy violation threshold/offset.
# Smaller values restrict the specific entropy minimum preservation more tightly, while larger
# values relax the violation check to prevent numerical dissipation (e.g. 1e-4, 1e-2).
ENTROPY_LIMITER_EPS = 1e-4

[PPR]
# ENABLE_PPR: Enable Phantom Pressure Regularization (PPR) for shock capturing.
ENABLE_PPR = false

# PPR_THETA: Extrapolation factor parameter (theta >= 0). 
# Used to calculate the regularized pressure: P_reg = P_phys + theta * (P_phys - P_phan).
# Larger values increase the level of shock regularization.
PPR_THETA = 0.55

# PPR_C_TAU: Scaling coefficient for the relaxation time tau.
# Used to compute relaxation timescale: tau = PPR_C_TAU * h / (||u|| + a).
# Typically in the range 0.01 to 1.0.
PPR_C_TAU = 0.1

[ImmersedBoundary]
# ENABLE_IB: Toggle the Immersed Boundary Method for solid boundaries
ENABLE_IB = false

# ENABLE_SBM_DIAGNOSTICS: Toggle runtime diagnostics for SBM extrapolation quality.
# Outputs to sbm_diagnostics.csv at every RESTART_INTERVAL.
ENABLE_SBM_DIAGNOSTICS = false

# IB_DL_SCALE: Scale factor for the shifted boundary 1D donor interval length (dL).
# Can be tuned if the SBM extrapolations suffer from Gibbs oscillations.
# Note: Aligning with the Colombo paper, dL is independent of boundary distance D.
IB_DL_SCALE = 1.0

# IB_L_SCALE: Scale factor for the shifted boundary 1D donor offset distance (L).
# Allows shrinking the distance from the shifted boundary to the first donor node.
IB_L_SCALE = 1.0

# ENABLE_IB_3C: Toggle Colombo Constraint 3.c. Set to false for solid airfoils to prevent boundary loss.
ENABLE_IB_3C = false

# IB_METHOD: Immersed Boundary method ("SBM", "VPM_ANALYTICAL", "VPM_EXPLICIT")
IB_METHOD = SBM

# IB_SHAPE: Geometry definition. Supported: "CIRCLE", "NACA", "MULTI"
#   - CIRCLE: Uses standard single cylinder geometry.
#   - NACA: Uses a standard 4-digit airfoil geometry.
#   - MULTI: Allows user-defined lists of convex quadrilaterals & lines/parabolas.
IB_SHAPE = CIRCLE

# IB_NACA_CODE: Standard 4-digit NACA airfoil code (e.g. "0012", "2412"). Used if IB_SHAPE = NACA.
IB_NACA_CODE = 0012

# IB_AOA: Airfoil angle of attack (AOA) in degrees. Used if IB_SHAPE = NACA.
IB_AOA = 0.0

# Geometry dimensions of the cylinder/airfoil (Used if IB_SHAPE = CIRCLE or NACA)
# For CIRCLE: Center coordinate (IB_CENTER_X, IB_CENTER_Y) and Radius (IB_RADIUS).
# For NACA: Leading edge coordinate (IB_CENTER_X, IB_CENTER_Y) and Chord length (IB_RADIUS).
IB_CENTER_X = 2.0
IB_CENTER_Y = 1.0
IB_RADIUS = 0.25

# IB_CHORD: Reference chord length for Lift/Drag force coefficients (typically matches airfoil chord IB_RADIUS/length).
IB_CHORD = 1.0

# --- MULTI Shape Options (Used if IB_SHAPE = MULTI) ---
# IB_Q_TIME_MAP: Piecewise-linear checkpoints for time parameter q(t) (format: t_0:q_0 t_1:q_1 ...).
# Useful for prescribing moving/morphing geometries. Default is 0.0:0.0.
# Example: 0.0:0.0 1.0:0.0 2.0:1.0 (remains at q=0 from t=0 to 1, then linearly increases to q=1 at t=2)
IB_Q_TIME_MAP = 0.0:0.0 1.0:0.0 2.0:1.0

# IB_NUM_QUADS: Number of convex quadrilaterals to mask out.
IB_NUM_QUADS = 0
# IB_QUAD_i: 8 coordinates representing ordered vertices (x1 y1 x2 y2 x3 y3 x4 y4) for quadrilateral i.
# IB_QUAD_0 = 0.2 0.2 0.4 0.2 0.4 0.4 0.2 0.4

# IB_NUM_POLYS: Number of lines/parabolas to mask out.
IB_NUM_POLYS = 0
# IB_POLY_i: Defines curve f(xi) = a*xi^2 + b*xi + c where coefficients interpolate linearly with q(t).
# Format: DIR a0 b0 c0 a1 b1 c1 SIDE
#   - DIR: 'Y' for vertical offset f(x) = y, 'X' for horizontal offset f(y) = x
#   - a0, b0, c0: Coefficients [a, b, c] at state q = 0
#   - a1, b1, c1: Coefficients [a, b, c] at state q = 1
#   - SIDE: Which side of the curve to mask out: "ABOVE", "BELOW" (for DIR=Y) or "LEFT", "RIGHT" (for DIR=X)
# Example: Y 0.0 0.0 0.2 0.0 0.0 0.4 BELOW
#   (defines a flat horizontal line moving vertically from y=0.2 at q=0 to y=0.4 at q=1, masking below it)
# IB_POLY_0 = Y 0.0 0.0 0.2 0.0 0.0 0.4 BELOW

# IB_SHARP: Use a sharp solid boundary interface mask (true) or regularized Heaviside (false).
# For SBM, this determines whether to output sharp diagnostic representations.
IB_SHARP = true

# IB_SMOOTH_WIDTH: Half-width thickness of the smoothed Heaviside boundary interface 
# (measured in units of grid spacing dx, dy). Typically 1.0 to 2.0.
IB_SMOOTH_WIDTH = 1.5

# --- Tree Decomposition (Quadtree Adaptive Mesh Refinement) ---
[TreeDecomposition]
# Target refinement level for cells touching a wall boundary. Set to 0 to disable near-wall refinement.
# Individual boundaries in domain.grid can opt-out by appending ":NOREFINED" (e.g. WALL:NOREFINED).
WALL_REFINEMENT_LEVEL = 2

# Number of cell layers from the wall boundary to apply the WALL_REFINEMENT_LEVEL.
WALL_REFINEMENT_CELLS = 3

# Number of manual refinement zones defined. Supports "NUM_REFINEMENT_ZONES" or "WALL_REFINEMENT_ZONES".
NUM_REFINEMENT_ZONES = 1

# --- Refinement Zone Configuration ---
# Each zone i (from 0 to NUM_REFINEMENT_ZONES-1) is configured using:
#   - ZONE_i_SHAPE: Geometry shape ("CIRCLE", "NACA", "BOX", or "POLYGON")
#   - ZONE_i_LEVEL: Target refinement level for cells overlapping this zone.
#
# Shape-specific parameters:
#   For CIRCLE:
#     - ZONE_i_CENTER_X, ZONE_i_CENTER_Y: Center coordinates.
#     - ZONE_i_RADIUS: Circle radius.
#
#   For NACA:
#     - ZONE_i_CENTER_X, ZONE_i_CENTER_Y: Leading edge coordinates.
#     - ZONE_i_RADIUS: Chord length.
#     - ZONE_i_NACA_CODE: 4-digit airfoil code (e.g. "0012").
#     - ZONE_i_AOA: Angle of attack in degrees.
#
#   For BOX:
#     - ZONE_i_CENTER_X, ZONE_i_CENTER_Y: Box center coordinates.
#     - ZONE_i_WIDTH: Width (X-direction size).
#     - ZONE_i_HEIGHT: Height (Y-direction size).
#
#   For POLYGON:
#     - ZONE_i_POLY_X: Whitespace-separated X coordinates of polygon vertices.
#     - ZONE_i_POLY_Y: Whitespace-separated Y coordinates of polygon vertices.
#
# Example Circle Zone:
ZONE_0_SHAPE = CIRCLE
ZONE_0_CENTER_X = 0.5
ZONE_0_CENTER_Y = 0.5
ZONE_0_RADIUS = 0.25
ZONE_0_LEVEL = 2

# Example Box Zone:
# ZONE_1_SHAPE = BOX
# ZONE_1_CENTER_X = 1.5
# ZONE_1_CENTER_Y = 0.5
# ZONE_1_WIDTH = 0.4
# ZONE_1_HEIGHT = 0.2
# ZONE_1_LEVEL = 3

# Example Polygon Zone:
# ZONE_2_SHAPE = POLYGON
# ZONE_2_POLY_X = 0.1 0.3 0.2
# ZONE_2_POLY_Y = 0.1 0.1 0.3
# ZONE_2_LEVEL = 2

[IO]
# OUTPUT_INTERVAL: Time interval between full, continuous VTK plotting snapshots (plot_*.vtm).
OUTPUT_INTERVAL = 0.01

# RESTART_INTERVAL: Time interval between exact, lossless restart checkpoints (sol_*.vtm).
RESTART_INTERVAL = 0.1

# RESIDUAL_INTERVAL: Time interval between L2 residual tracking outputs to csv_outputs/residuals.csv
RESIDUAL_INTERVAL = 0.005

# PROBE_INTERVAL: Time interval between point probe tracking to probe.csv
PROBE_INTERVAL = 0.005

# PRINT_INTERVAL: Time interval between terminal stdout status updates.
PRINT_INTERVAL = 0.01

# RESTART_FILE: Path to a .vts file to resume from. Leave blank if not restarting.
RESTART_FILE = 

# RESTART_TIME: Simulation time corresponding to the restart file.
RESTART_TIME = 0.0

[Probes]
# Define point probes to extract time-history data.
# Format: PROBE_NAME = X, Y, Variable
# Available variables: Density, XMomentum, YMomentum, Energy, Pressure, Temperature, Mach, Sigma
PROBE_1 = 0.5, 0.5, Pressure
PROBE_2 = 0.1, 0.5, Density
PROBE_3 = 0.9, 0.5, Mach

################################################################################
# DOMAIN GRID SYNTAX (domain.grid)
################################################################################
# The simulation domain is defined in domain.grid using multiple [BlockN] 
# sections. Each block is a Cartesian grid with its own resolution and BCs.
#
# Example [Block0] configuration:
# [Block0]
# N_ELEM_X = 20           # Number of elements in X direction
# N_ELEM_Y = 20           # Number of elements in Y direction
# X_MIN = 0.0             # Physical X coordinate of the left face
# X_MAX = 1.0             # Physical X coordinate of the right face
# Y_MIN = 0.0             # Physical Y coordinate of the bottom face
# Y_MAX = 1.0             # Physical Y coordinate of the top face
# 
# BC_L = TRANSMISSIVE    # Boundary Condition for Left face
# BC_R = 1:L             # Connects to Block 1's Left face
# BC_B = 0:T             # Connects to Block 0's Top face (self-periodic)
# BC_T = 0:B             # Connects to Block 0's Bottom face (self-periodic)
#
# Available BCs:
#   - OUTFLOW_SUPERSONIC: Simple copy-out/ghost-state condition.
#   - WALL: Reflective slip-wall (normal velocity negated).
#   - WALL_NOSLIP: Adiabatic viscous no-slip wall (u=v=0, dT/dn=0).
#   - WALL_NOSLIP:T: Isothermal viscous no-slip wall at temperature T.
#   - WALL_MOVING:V: Adiabatic moving wall with tangential velocity V.
#   - WALL_MOVING:V:T: Isothermal moving wall with velocity V and temperature T.
#   - INFLOW_SUPERSONIC:rho:u:v:p: Enforces the given reference state.
#   - CHARACTERISTIC:rho:u:v:p: Riemann invariant based subsonic inflow/outflow using the reference state.
#   - TOTAL_PRESSURE_COMP:Pt: Subsonic backpressure boundary scaling the state to match target compressible total pressure Pt.
#   - TOTAL_PRESSURE_INCOMP:Pt: Subsonic backpressure boundary using Bernoulli's incompressible equation to match target total pressure Pt.
#   - STATIC_PRESSURE:P: Subsonic backpressure boundary enforcing the target static pressure P while copying interior density and velocity.
#   - ID:FACE: Connects to the specified face of neighbor Block ID.
#     Example: '2:T' means "Connect to the TOP face of Block 2".
#     Note: For periodic boundaries, simply map opposite faces to each other 
#     (e.g., Block 0 Left connects to Block 0 Right using '0:R').
#
#   Near-Wall Refinement Suffix Flags:
#     Any physical wall boundary (WALL, WALL_NOSLIP, WALL_MOVING) can optionally 
#     append a toggle suffix to control near-wall adaptive mesh refinement (AMR):
#       - :NOREFINED (or :NOREFINEMENT, :NO_REFINE) - disables near-wall refinement
#       - :REFINED (or :REFINE) - explicitly enables near-wall refinement (default)
#     Example: "BC_B = WALL:NOREFINED" ignores the bottom boundary during near-wall AMR.
################################################################################
