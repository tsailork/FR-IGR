/**
 * @file sensor.hpp
 * @brief Redirection hook and placeholder for the IGR Shock Sensor subsystem.
 *
 * @note The shock sensor computation is implemented as member functions of the 
 * main `Solver` class:
 * - `Solver::compute_sensor_source()`
 *
 * Its corresponding declaration is located in `solver.hpp`, and its implementation 
 * is located in `sensor.cpp`. This file acts as a placeholder to preserve 
 * compilation unit boundaries.
 *
 * @see solver.hpp
 * @see sensor.cpp
 */

#pragma once
